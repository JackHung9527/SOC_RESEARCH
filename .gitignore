.claude/

# firmware-project artefacts (root-level publish dir; populated by MCU/Makefile)
/BUILD/

# TOOLS QA scratch outputs (generated PNGs / temp pptx for visual review)
/TOOLS/pptx/qa/
/TOOLS/pptx/__pycache__/

# project.yaml audit trail (audit lives in git history, not committed .bak)
*.bak

# Stray vendored HAL clone (canonical location is MCU/soc_research_mcu/Drivers/)
/cmsis_g0/

# CubeIDE / Make build output (under the CubeIDE project subdir)
/MCU/soc_research_mcu/build/

# Editor / IDE caches (any directory)
.vscode/
.idea/
*.swp
*~
